{
  "name": "pw-cucumber-ts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "engines": {
    "node": ">=20 <25",
    "npm": ">=10"
  },
  "scripts": {
    "test": "ts-node src/runner.ts",
    "report": "ts-node src/report/generate-report.ts && ts-node src/report/rotate-reports.ts",
    "setup": "npm install && npx playwright install",
    "setup:clean": "npm ci && npx playwright install --with-deps",
    "check:env": "node -v && npm -v && npx playwright --version"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@cucumber/cucumber": "^12.6.0",
    "@playwright/test": "^1.58.0",
    "@types/archiver": "^7.0.0",
    "archiver": "^7.0.1",
    "cross-env": "^10.1.0",
    "cucumber-html-reporter": "^7.2.0",
    "dotenv": "^17.2.3",
    "moment": "^2.30.1",
    "playwright": "^1.58.0",
    "rimraf": "^6.1.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
