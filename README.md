## Setup on a new machine

1. Install Node (recommended: Node 20)
2. In project root run:
   npm run setup

To run tests:
npm test

## Setup (new machine)

```bash
npm run setup
npm test
npm run check:env

```
